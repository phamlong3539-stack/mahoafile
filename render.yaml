services:
  - type: web
    name: ciphervault-pro
    runtime: python3
    buildCommand: "echo 'Building...'"
    startCommand: "python -m http.server $PORT"
    envVars:
      - key: PORT
        value: 3000
